import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
    height: 50,
    backgroundColor: '#1d56ff',
    alignItems: 'center',
    flexDirection: 'row',
  },
  title: { color: 'white', fontSize: 22, fontWeight: '800', marginLeft: 10 },
  icon: { height: 20, width: 20, margin: 10 },
});

export default styles;
